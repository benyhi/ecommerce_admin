import { ResourcePage } from "@/modules/resources/ResourcePage";

export const metadata = {
  title: "Usuarios",
};

export default function UsuariosPage() {
  return <ResourcePage resource="usuarios" />;
}
