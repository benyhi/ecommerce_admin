import { ResourcePage } from "@/modules/resources/ResourcePage";

export const metadata = {
  title: "Clientes",
};

export default function ClientesPage() {
  return <ResourcePage resource="clientes" />;
}
